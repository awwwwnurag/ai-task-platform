# AI Task Frontend React SPA

Please refer to the main repository `README.md` located sequentially in the root directory (`../README.md`) for expansive comprehensive guides detailing cluster orchestrations, Docker Compose deployment mechanisms, and overarching Kubernetes architectural blueprints.

To run specifically just the local frontend organically:
```bash
npm install
npm run dev
```
Navigate natively to `http://localhost:5173` referencing backend node triggers mapping dynamically against `https://ai-task-backend-xt0g.onrender.com/api`.
